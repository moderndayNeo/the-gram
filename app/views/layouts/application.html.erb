<!DOCTYPE html>
<html lang="en">
  <html>
    <head>
      <title>The Gram</title>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0">
      <meta name="description" content="The Gram is a clone of instagram">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>
      <%= stylesheet_link_tag "application", media: "all" %>
      <%= javascript_pack_tag "application" %>
      <% if logged_in? %>
        <script type="text/javascript">
          window.currentUser =
            <%= render("api/users/user.json.jbuilder", user: current_user).html_safe %>
        </script>
      <% end %>
    </head>
    <body>
      <%= yield %>
      <script type="text/javascript">
        window.mainLogoUrl = "<%= asset_url('the-gram-logo.png') %>"
        window.loginPageImgUrl = "<%= asset_url('screenshots/login_page.png') %>"
        window.appStoreLinkImg = "<%= asset_url('app-store-download-link.png') %>"
        window.playStoreLinkImg = "<%= asset_url('play-store-download-link.png') %>"
        window.placeholderImg = "<%= asset_url('placeholder-img.jpeg') %>"
        window.noAvatarImg = "<%= asset_url('icons/no_avatar.jpg') %>"
      </script>
    </body>
  </html>
